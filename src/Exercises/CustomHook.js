import React, { useState, useEffect } from 'react'

// Topic: Custom Hook
// Practice ideas:
//   - useLocalStorage(key, initialValue) — sync state with localStorage
//   - useFetch(url) — returns { data, loading, error }
//   - useDebounce(value, delay) — debounce a rapidly changing value
//   - useToggle(initial) — returns [value, toggle]

// your custom hook here

export function CustomHook() {
  return (
    <div style={{ padding: 24 }}>
      <h2>Custom Hook</h2>
      {/* write your solution below */}
    </div>
  )
}
