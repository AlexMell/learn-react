import React, { useState, useRef } from 'react'

// Topic: useRef
// Practice ideas:
//   - focus an input programmatically
//   - store previous value of state without triggering re-render
//   - count renders without causing a re-render

export function UseRef() {
  return (
    <div style={{ padding: 24 }}>
      <h2>useRef</h2>
      {/* write your solution below */}
    </div>
  )
}
