import React, { useState, useContext, createContext } from 'react'

// Topic: useContext
// Practice ideas:
//   - create a ThemeContext (light/dark) and consume it in a nested component
//   - create an AuthContext with a user object
//   - pass a setter through context so a deep child can update shared state

export function UseContext() {
  return (
    <div style={{ padding: 24 }}>
      <h2>useContext</h2>
      {/* write your solution below */}
    </div>
  )
}
