import React, { useState, useMemo, useCallback } from 'react'

// Topic: useMemo / useCallback
// Practice ideas:
//   - memoize an expensive filter/sort over a list
//   - wrap a handler with useCallback so a child wrapped in React.memo skips re-render
//   - notice when memoization actually helps vs adds noise

export function UseMemo() {
  return (
    <div style={{ padding: 24 }}>
      <h2>useMemo / useCallback</h2>
      {/* write your solution below */}
    </div>
  )
}
